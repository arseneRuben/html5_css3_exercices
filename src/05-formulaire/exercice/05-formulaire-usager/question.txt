
Objectifs:
    - Création d'un formulaire complet contenu dans une balise form en utilisant un regroupement avec fieldset.

Consignes:
    - Créez un fichier formulaire-usager.html en utilisant tous les balises nécessaires pour obtenir le même résultat que la maquette.
    - Pour la mise en forme des retours à la ligne, vous pouvez insérer chaque ligne du formulaire dans une balise <div>.
